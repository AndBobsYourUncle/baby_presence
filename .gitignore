.env
.venv/
.claude/settings.local.json
__pycache__/
*.pyc

# Model artifacts (YOLO weights downloaded at runtime, trained classifier + sidecar)
*.pt
presence_model.json

# Logged frames for future fine-tuning
frames/

# Labeling artifacts + training dataset (personal, camera-specific)
ranges.csv
*.markers.json
dataset/
